spring:
  application:
    name: notes
  profiles:
    active: dev
  config:
    import: optional:configserver:http://192.168.0.102:8888

resilience4j:
  circuitbreaker:
    instances:
      persons:
        properties:
          minimumNumberOfCalls: 500
feign:
  circuitbreaker:
    enabled: true
